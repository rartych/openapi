description: Unauthorized
content:
  application/json:
    schema:
      $ref: ../schemas/ErrorInfo.yaml
    example:
      status: 401
      code: UNAUTHENTICATED
      message: 'Authorization failed: ...'
