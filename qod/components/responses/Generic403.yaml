description: Forbidden
content:
  application/json:
    schema:
      $ref: ../schemas/ErrorInfo.yaml
    example:
      status: 403
      code: PERMISSION_DENIED
      message: 'Operation not allowed: ...'
