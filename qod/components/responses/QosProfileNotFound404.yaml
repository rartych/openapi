description: Qos Profile not found
content:
  application/json:
    schema:
      $ref: ../schemas/ErrorInfo.yaml
    example:
      status: 404
      code: NOT_FOUND
      message: QosProfile Id does not exist
