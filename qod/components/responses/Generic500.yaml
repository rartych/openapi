description: Internal server error
content:
  application/json:
    schema:
      $ref: ../schemas/ErrorInfo.yaml
    example:
      status: 500
      code: INTERNAL
      message: 'Internal server error: ...'
